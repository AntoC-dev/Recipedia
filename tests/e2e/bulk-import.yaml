platform:
  ios:
    disableAnimations: true
  android:
    disableAnimations: true

flows:
  - cases/bulk-import/*

executionOrder:
  flowsOrder:
    - "1 - Bulk Import HelloFresh Flow (French)"
    - "2 - Bulk Import Seen Recipes (French)"
    - "3 - Bulk Import Quitoque with Language Filter (French)"
    - "4 - Bulk Import with Skipped Recipe (all ingredients rejected)"
