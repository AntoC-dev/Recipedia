appId: "com.recipedia"
---
# FAB IDs use regex pattern because React Native Paper adds "-container-outer-layer" suffix on iOS
- tapOn:
    id: "ReduceButton.*"
    label: "Collapse FAB menu"

- waitForAnimationToEnd:
    timeout: 3000
    label: "Wait for FAB collapse animation"
