appId: "com.recipedia"
---
- tapOn:
    id: "VerticalBottomButtons::UrlDialog::Input::CustomTextInput"
    label: "Focus on URL input field"

- runFlow:
    file: "../../../waitForKeyboard.yaml"
    label: "Wait for keyboard to appear"

- inputText: "${RECIPE_URL}"

- waitForAnimationToEnd:
    label: "Wait for keyboard animation"

- assertVisible:
    id: "VerticalBottomButtons::UrlDialog::Input::CustomTextInput"
    text: "${RECIPE_URL}"
    label: "Assert - Check that the URL is well entered in the text input"

- runFlow:
    file: "../../../../flows/dismissModalKeyboard.yaml"
    label: "Dismiss modal keyboard (platform-specific)"

- runFlow:
    file: "../../../waitForKeyboardDismiss.yaml"
    label: "Wait for keyboard to dismiss"

- tapOn:
    id: "VerticalBottomButtons::UrlDialog::SubmitButton"
    label: "Submit URL for scraping"

- extendedWaitUntil:
    visible:
      id: "RecipeImage"
    timeout: 90000
    label: "Wait for recipe screen to load after scraping"

- extendedWaitUntil:
    visible:
      id: "RecipeTitle::CustomTextInput"
    timeout: 5000
    label: "Wait for recipe title to be populated"
