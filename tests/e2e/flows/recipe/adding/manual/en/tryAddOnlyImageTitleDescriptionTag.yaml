appId: "com.recipedia"
---

- waitForAnimationToEnd

- scrollUntilVisible:
    element:
      id: "RecipeTags::RoundButton"
    direction: DOWN
    speed: 40
    centerElement: true
    label: "Scroll to tags's adding button"

- waitForAnimationToEnd

- tapOn:
    id: "RecipeTags::RoundButton"
    label: "Add a new tag"

- waitForAnimationToEnd

- scrollUntilVisible:
    element:
      id: "RecipeTags::List::0::CustomTextInput"
    direction: UP
    speed: 20
    label: "Scroll to ensure firsttag's text input is visible"

- waitForAnimationToEnd

- tapOn:
    id: "RecipeTags::List::0::CustomTextInput"
    label: "Press the new tag TextInput"

- inputText:
    text: "Japanese"
    label: "Write the tag in the input text"

- waitForAnimationToEnd:
    label: "Wait for keyboard animation"

- pressKey: enter

- waitForAnimationToEnd:
    label: "Wait for keyboard to dismiss"
