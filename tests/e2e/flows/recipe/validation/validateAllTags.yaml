appId: "com.recipedia"
---
- repeat:
    while:
      visible:
        id: "${PREFIX}::ValidationQueue::Tag::SimilarityDialog::Title"
    commands:
      - runFlow:
          file: "validateOneTag.yaml"
          env:
            PREFIX: "${PREFIX}"
          label: "Validate one tag"
