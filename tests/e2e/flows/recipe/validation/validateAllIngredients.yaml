appId: "com.recipedia"
---
- repeat:
    while:
      visible:
        id: "${PREFIX}::ValidationQueue::Ingredient::SimilarityDialog::Title"
    commands:
      - runFlow:
          file: "validateOneIngredient.yaml"
          env:
            PREFIX: "${PREFIX}"
          label: "Validate one ingredient"
