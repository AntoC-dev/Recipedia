appId: "com.recipedia"
---
# Launch from a cleared state so each per-screen case is fully independent and
# can run on its own. This is the same cold-launch + skip-onboarding + seed-wait
# + Home/FAB readiness sequence the seed case uses, so delegate to it rather than
# duplicating the steps.
- runFlow:
    file: "../setupPerformanceTest.yaml"
    label: "Fresh launch, skip onboarding, wait for seed and Home"
