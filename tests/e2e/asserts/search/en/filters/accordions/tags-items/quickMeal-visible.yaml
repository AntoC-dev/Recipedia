appId: "com.recipedia"

---

- waitForAnimationToEnd

- scrollUntilVisible:
    element:
      id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${ITEM_INDEX}"
    direction: DOWN
    speed: 60
    centerElement: true
    label: "Scroll to Quick Meal filter item"

- waitForAnimationToEnd

- assertVisible:
    id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${ITEM_INDEX}"
    label: "Quick Meal filter is visible"

- evalScript: ${output.itemIndex = output.itemIndex + 1}
