appId: "com.recipedia"
---
- scrollUntilVisible:
    element:
      id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${output.itemIndex}"
    direction: DOWN
    speed: 60
    centerElement: true
    label: "Scroll to Pine Nuts filter item"

- assertVisible:
    id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${output.itemIndex}"
    label: "Pine Nuts filter is visible"

- evalScript: ${output.itemIndex = output.itemIndex + 1}
