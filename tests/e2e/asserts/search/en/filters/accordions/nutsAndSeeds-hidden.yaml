appId: "com.recipedia"
---
# Assert accordion is NOT displayed
- runFlow:
    when:
      platform: Android
    commands:
      - assertNotVisible:
          text: "Nuts and Seeds"
          label: "Nuts and Seeds accordion not displayed on Android"

- runFlow:
    when:
      platform: iOS
    commands:
      - assertNotVisible:
          text: "Nuts and Seeds 󰅀"
          label: "Nuts and Seeds accordion not displayed on iOS"
