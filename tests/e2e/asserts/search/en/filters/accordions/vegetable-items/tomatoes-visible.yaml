appId: "com.recipedia"
---

- waitForAnimationToEnd

- scrollUntilVisible:
    element:
      id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${output.itemIndex}"
    direction: DOWN
    speed: 60
    centerElement: true
    label: "Scroll to Tomatoes filter item"

- waitForAnimationToEnd

- assertVisible:
    id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${output.itemIndex}"
    label: "Tomatoes filter is visible"

- evalScript: ${output.itemIndex = output.itemIndex + 1}
