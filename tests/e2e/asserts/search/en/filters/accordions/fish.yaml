appId: "com.recipedia"
---
# Route by STATE to the shared accordion asserts for the Fish category.
- runFlow:
    when:
      true: ${STATE == "filtered"}
    file: "accordionFiltered.yaml"
    env:
      CATEGORY: "Fish"
      ITEM_NAMES: "Salmon"
      ITEM_COUNT: "1"
      VISIBLE_ITEMS: ${VISIBLE_ITEMS}

- runFlow:
    when:
      true: ${STATE == "hidden"}
    file: "accordionHidden.yaml"
    env:
      CATEGORY: "Fish"

- runFlow:
    when:
      true: ${STATE == "all"}
    file: "accordionAll.yaml"
    env:
      CATEGORY: "Fish"
      ITEM_COUNT: "1"
