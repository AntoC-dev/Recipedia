appId: "com.recipedia"
---
# Route by STATE to the shared accordion asserts for the Sugar category.
- runFlow:
    when:
      true: ${STATE == "filtered"}
    file: "accordionFiltered.yaml"
    env:
      CATEGORY: "Sugar"
      ITEM_NAMES: "Sugar"
      ITEM_COUNT: "1"
      VISIBLE_ITEMS: ${VISIBLE_ITEMS}

- runFlow:
    when:
      true: ${STATE == "hidden"}
    file: "accordionHidden.yaml"
    env:
      CATEGORY: "Sugar"

- runFlow:
    when:
      true: ${STATE == "all"}
    file: "accordionAll.yaml"
    env:
      CATEGORY: "Sugar"
      ITEM_COUNT: "1"
