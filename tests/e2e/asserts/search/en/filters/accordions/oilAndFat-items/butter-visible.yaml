appId: "com.recipedia"
---
- scrollUntilVisible:
    element:
      id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${output.itemIndex}"
    direction: DOWN
    speed: 60
    centerElement: true
    label: "Scroll to Butter filter item"

- assertVisible:
    id: "SearchScreen::FilterAccordion::Accordion::${output.accordionIndex}::Item::${output.itemIndex}"
    label: "Butter filter is visible"

- evalScript: ${output.itemIndex = output.itemIndex + 1}
