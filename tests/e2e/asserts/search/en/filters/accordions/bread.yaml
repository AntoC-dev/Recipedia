appId: "com.recipedia"
---
# Route by STATE to the shared accordion asserts for the Bread category.
- runFlow:
    when:
      true: ${STATE == "hidden"}
    file: "accordionHidden.yaml"
    env:
      CATEGORY: "Bread"

- runFlow:
    when:
      true: ${STATE == "all"}
    file: "accordionAll.yaml"
    env:
      CATEGORY: "Bread"
      ITEM_COUNT: "1"
