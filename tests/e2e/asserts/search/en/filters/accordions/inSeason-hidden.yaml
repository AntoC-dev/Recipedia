appId: "com.recipedia"
---
# Assert accordion is NOT displayed
- runFlow:
    when:
      platform: Android
    commands:
      - assertNotVisible:
          text: "Only in-season ingredients"
          label: "Only in-season ingredients accordion not displayed on Android"

- runFlow:
    when:
      platform: iOS
    commands:
      - assertNotVisible:
          text: "Only in-season ingredients 󰅀"
          label: "Only in-season ingredients accordion not displayed on iOS"
