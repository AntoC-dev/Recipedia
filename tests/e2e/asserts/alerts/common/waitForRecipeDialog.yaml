appId: "com.recipedia"
---
# Saving downscales the picked image first, which has been measured at 40s on a loaded CI runner.
- extendedWaitUntil:
    visible:
      id: "Recipe::Dialog::Title"
    timeout: 90000
    label: "Wait for the post-validation dialog to open"
