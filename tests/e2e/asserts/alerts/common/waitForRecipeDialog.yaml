appId: "com.recipedia"
---
# 30s, not assertVisible's implicit default: callers retry twice, so every second is paid 3x.
- extendedWaitUntil:
    visible:
      id: "Recipe::Dialog::Title"
    timeout: 30000
    label: "Wait for the post-validation dialog to open"
