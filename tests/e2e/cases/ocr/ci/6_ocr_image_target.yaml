name: "6 - OCR Image Target Flow"
appId: "com.recipedia"
tags:
  - ocr
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/recipe/adding/ocr/checkIfCleanIsNeeded.yaml"
          label: "Setup - Check if clean is needed"
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../6_ocr_image_target.yaml"
          label: "Run 6 - OCR Image Target Flow"
