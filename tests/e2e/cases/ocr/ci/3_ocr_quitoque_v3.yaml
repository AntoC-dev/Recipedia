name: "3 - OCR Quitoque V3 Camera Flow"
appId: "com.recipedia"
tags:
  - ocr
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/recipe/adding/ocr/checkIfCleanIsNeeded.yaml"
          label: "Setup - Check if clean is needed"
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../3_ocr_quitoque_v3.yaml"
          label: "Run 3 - OCR Quitoque V3 Camera Flow"
