name: "2 - OCR Quitoque V1 Gallery Flow"
appId: "com.recipedia"
tags:
  - ocr
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/recipe/adding/ocr/checkIfCleanIsNeeded.yaml"
          label: "Setup - Check if clean is needed"
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../2_ocr_quitoque_v1.yaml"
          label: "Run 2 - OCR Quitoque V1 Gallery Flow"
