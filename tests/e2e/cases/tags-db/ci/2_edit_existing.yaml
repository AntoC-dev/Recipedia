name: "2 - Edit Existing Tag Flow"
appId: "com.recipedia"
tags:
  - database
  - tags-db
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../2_edit_existing.yaml"
          label: "Run 2 - Edit Existing Tag Flow"
