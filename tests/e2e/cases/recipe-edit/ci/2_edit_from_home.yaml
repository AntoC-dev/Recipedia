name: "2 - Recipe Edit From Home Flow"
appId: "com.recipedia"
tags:
  - recipe-edit
env:
  recipeName: "Sushi Rolls"
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../2_edit_from_home.yaml"
          label: "Run 2 - Recipe Edit From Home Flow"
