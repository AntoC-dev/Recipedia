name: "3 - Duplicate Recipe Close Quantities Detection Flow"
appId: "com.recipedia"
tags:
  - duplicates
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../3_recipe_close_quantities.yaml"
          label: "Run 3 - Duplicate Recipe Close Quantities Detection Flow"
