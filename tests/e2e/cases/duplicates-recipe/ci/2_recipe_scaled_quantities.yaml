name: "2 - Duplicate Recipe Scaled Quantities Detection Flow"
appId: "com.recipedia"
tags:
  - duplicates
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../2_recipe_scaled_quantities.yaml"
          label: "Run 2 - Duplicate Recipe Scaled Quantities Detection Flow"
