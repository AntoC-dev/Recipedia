name: "1 - Duplicate Tag Unique Detection Flow"
appId: "com.recipedia"
tags:
  - duplicates
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../1_tag_unique.yaml"
          label: "Run 1 - Duplicate Tag Unique Detection Flow"
