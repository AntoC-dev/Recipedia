name: "2 - Duplicate Tag Fuzzy Detection Flow"
appId: "com.recipedia"
tags:
  - duplicates
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../2_tag_fuzzy.yaml"
          label: "Run 2 - Duplicate Tag Fuzzy Detection Flow"
