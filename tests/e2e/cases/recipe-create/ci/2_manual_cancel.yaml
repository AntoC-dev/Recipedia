name: "2 - Manual Recipe Creation Cancel Flow"
appId: "com.recipedia"
tags:
  - recipe-create
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../2_manual_cancel.yaml"
          label: "Run 2 - Manual Recipe Creation Cancel Flow"
