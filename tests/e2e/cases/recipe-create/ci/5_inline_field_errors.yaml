name: "5 - Recipe Create Inline Field Errors Flow"
appId: "com.recipedia"
tags:
  - recipe-create
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../5_inline_field_errors.yaml"
          label: "Run 5 - Recipe Create Inline Field Errors Flow"
