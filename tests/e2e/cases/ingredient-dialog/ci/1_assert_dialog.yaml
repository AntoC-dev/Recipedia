name: "1 - Assert Ingredient Dialog Structure"
appId: "com.recipedia"
tags:
  - dialog
  - ingredients-db
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../1_assert_dialog.yaml"
          label: "Run 1 - Assert Ingredient Dialog Structure"
