name: "3 - Season Filter Edge Cases Flow"
appId: "com.recipedia"
tags:
  - settings
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../3_season_filter_edge_cases.yaml"
          label: "Run 3 - Season Filter Edge Cases Flow"
