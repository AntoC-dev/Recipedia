name: "4 - Default Persons Setting Flow"
appId: "com.recipedia"
tags:
  - settings
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../4_default_persons.yaml"
          label: "Run 4 - Default Persons Setting Flow"
