name: "1 - Web Invalid URL"
appId: "com.recipedia"
tags:
  - web
  - edge-case
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../1_web_invalid_url.yaml"
          label: "Run 1 - Web Invalid URL"
