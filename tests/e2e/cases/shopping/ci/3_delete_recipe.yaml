name: "3 - Delete Recipe From Shopping Flow"
appId: "com.recipedia"
tags:
  - shopping
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../3_delete_recipe.yaml"
          label: "Run 3 - Delete Recipe From Shopping Flow"
