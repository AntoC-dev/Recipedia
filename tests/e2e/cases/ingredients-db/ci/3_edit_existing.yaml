name: "3 - Edit Existing Ingredient Flow"
appId: "com.recipedia"
tags:
  - database
  - ingredients-db
---
- retry:
    maxRetries: 2
    commands:
      - runFlow:
          file: "../../../flows/setupTest.yaml"
          label: "Setup - Launch app and wait for ready"
      - runFlow:
          file: "../3_edit_existing.yaml"
          label: "Run 3 - Edit Existing Ingredient Flow"
