# Python dependencies for iOS Pyodide recipe scraping.
#
# Downloaded at build time and bundled into the Pyodide HTML bundle for offline
# installation. Pyodide version is managed via npm (package.json).

recipe-scrapers==15.11.0
